import React from 'react'
import { Text, View } from 'react-native'

const Book = () => {
    return (
        <View style={{flex: 1, justifyContent: 'center'}}>
            <Text>Book</Text>
        </View>
    )
}

export default Book